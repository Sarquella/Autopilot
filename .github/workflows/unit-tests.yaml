name: ✅ Unit Tests

on:
  pull_request:
    branches: ["main"]

jobs:
  build:
    name: Unit Tests
    runs-on: macos-13

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: 🔨 Xcode version
        run: |
          xcodebuild -version
      - name: ✅ Unit Tests
        run: |
           xcodebuild test -scheme Autopilot -destination 'platform=macOS,name=Any Mac'
